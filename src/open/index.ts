import { OptionsTS, JsonTypeEnum } from '../types'

export const genTSInterface = (data: any, type: JsonTypeEnum, options: OptionsTS): string => {
  console.log('---genTSCode', data, type, options);

  return '';
}
